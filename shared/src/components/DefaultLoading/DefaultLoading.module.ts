import DefaultLoading from "@shared/components/DefaultLoading/DefaultLoading";

import { createComponentMount } from "@shared/helpers/createComponentMount";

const { mount, unmount } = createComponentMount(DefaultLoading);

export { mount, unmount };
